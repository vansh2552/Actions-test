name: My Workflow

on:
  workflow_dispatch:
    inputs:
      continue_on_error:
        description: 'Continue on error?'
        required: true
        default: 'false'
        # Add any other relevant validation or options as needed

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "Job 1"
        
      - name: Step 2 (Continue on Error)
        run: echo "Job 1 Step 2"
        if: ${{ github.event.inputs.continue_on_error == 'true' }}

# Rest of the jobs...
