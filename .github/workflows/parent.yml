name: Parent Workflow

on:
  workflow_run:
    workflows: ["child-workflow-1"]
    types:
      - completed

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      # Other steps of the workflow
