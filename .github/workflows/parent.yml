name: Deploy
on:
  workflow_dispatch:
jobs:
  build:
    name: Run Web App
    runs-on: ubuntu-latest
    steps:
      - name: Build Web App
        run: |
          echo "Build Web App"
          sleep 10s
      - uses: convictional/trigger-workflow-and-wait@v1.3.0
        with:
          owner: vansh2552
          repo: Actions-test
          github_token: ${{ secrets.ACTIONS_SECRET }}
          workflow_file_name: child-workflow-1.yml
      - name: Deploy Web App
        run: |
          echo "Deploy Web App"
          sleep 10s
